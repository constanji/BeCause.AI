version: 1
name: BeCause
description: 因为智能

# 数据库配置（必需）
db:
  provider: mysql  # 或 postgresql, oracle, duckdb 等
  configuration:
    url: jdbc:mysql://localhost:3306/mall_demo
    username: root
    password: jinzijun
    timeout: 1 min

# LLM配置（必需）
llms:
  - name: default
    provider: deepseek  # 或 anthropic, ollama, gemini 等
    configuration:
      api-key: sk-f49ac4c972004801a72cbb9d42efe2a7
      model-name: deepseek-chat
      base-url: https://api.deepseek.com/v1

# 嵌入模型配置（可选）
embedding:
  provider: bge-small-zh-v15-q

# 代理配置
agents:
  - name: default
    description: default ask data agent
    provider: default