{
  "name": "@because/agents-types",
  "version": "3.0.44",
  "description": "Type definitions for @because/agents",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "peerDependencies": {
    "@because/agents": "^1.5.3"
  }
}
